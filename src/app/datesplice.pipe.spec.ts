import { DatesplicePipe } from './datesplice.pipe';

describe('DatesplicePipe', () => {
  it('create an instance', () => {
    const pipe = new DatesplicePipe();
    expect(pipe).toBeTruthy();
  });
});
